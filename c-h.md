Hi, I need help finishing my Sawari landing page project in this workspace.

Goal:
Make the website match my attached reference image exactly. The attached image is the final source of truth for both layout and content. PSD exports are only supporting assets and must not override the attached reference design.

Project files:
index.html, styles.css, script.js, server.js, assets/psd-export

What I need now:

Fix the Project Overview section image composition and spacing to match the reference.
Replace any placeholders or wrong images with original PSD-exported assets where available.
Finish the bottom area parity:
Contact strip
Register Your Interest form block
Documents strip with correct PDF icons/text alignment
Keep existing working interactions unless they conflict with the reference.
Run local validation and tell me exactly what changed.
Output format:

Short plan
Applied file changes
Visual parity notes (what is now matched, what still differs)
Next small step
Important:
Keep responses concise and avoid large repeated summaries to prevent request-size errors.