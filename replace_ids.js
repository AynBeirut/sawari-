const fs = require('fs'); let html = fs.readFileSync('floor-plans.html', 'utf8'); html = html.replace('id="galleryLightbox"', 'id="fpLightbox"'); html = html.replace('id="galleryLightboxClose"', 'id="fpLightboxClose"'); html = html.replace('id="galleryLightboxPrev"', 'id="fpLightboxPrev"'); html = html.replace('id="galleryLightboxImage"', 'id="fpLightboxImage"'); html = html.replace('id="galleryLightboxNext"', 'id="fpLightboxNext"'); html = html.replace('id="galleryLightboxProgress"', 'id="fpLightboxProgress"'); fs.writeFileSync('floor-plans.html', html, 'utf8'); console.log('IDs replaced');
